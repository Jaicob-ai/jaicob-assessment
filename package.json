{
    "name": "jaicob-assessment",
    "private": true,
    "scripts": {
        "dev": "turbo dev",
        "build": "turbo build",
        "lint": "turbo lint",
        "test": "turbo test",
        "dev:api": "turbo dev --filter=@app/api",
        "dev:web": "turbo dev --filter=@app/web"
    },
    "devDependencies": {
        "turbo": "^2.6.1",
        "typescript": "^5.9.3",
        "@types/node": "^20"
    },
    "workspaces": [
        "apps/*"
    ],
    "packageManager": "npm@10.9.0",
    "engines": {
        "node": ">=20.0.0"
    }
}
